#include "aoclib.h"

#include <stdlib.h>

FILE *get_data(size_t year, size_t day)
{
    FILE *fp = NULL;
    if (!fp)
    {
        fprintf(stderr, "get_data: input file missing\n");
        exit(-2);
    }

    return fp;
}
